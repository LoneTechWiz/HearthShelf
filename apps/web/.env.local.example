# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/personal_library

# Auth.js secret — generate with: openssl rand -base64 32
AUTH_SECRET=

# GitHub OAuth (or swap for another provider)
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
